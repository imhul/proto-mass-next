export const generateMapChunk = (position: all.game.Position, direction: "right" | "left" | "top" | "bottom") => {
    // console.info("New chunk at pos:", position, "dir:", direction)
    // TODO: 2. write function
}
