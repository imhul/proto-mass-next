// types
import type { gameTypes, storeTypes } from "@lib/types"

export const generateMapChunk = (position: gameTypes.Position, direction: "right" | "left" | "top" | "bottom") => {
    console.info("New chunk at pos:", position, "dir:", direction)
    // TODO: 2. write function
}
